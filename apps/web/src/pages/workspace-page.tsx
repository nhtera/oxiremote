import TerminalPage from './terminal-page'

// Phase 05 stub — workspace renders the existing single-session terminal so
// the new /h/:id/workspace landing route doesn't regress functionality.
// Phase 06 replaces this body with the sessions-list + multi-pane shell.
export default function WorkspacePage() {
  return <TerminalPage />
}
